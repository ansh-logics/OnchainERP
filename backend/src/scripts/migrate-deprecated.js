/**
 * Legacy npm script placeholder. One-time MongoDB → PostgreSQL migration was removed;
 * domain data lives in PostgreSQL only.
 */
console.log(
  'Deprecated: migrate script removed. PostgreSQL is the source of truth for domain data.'
);
process.exit(0);
